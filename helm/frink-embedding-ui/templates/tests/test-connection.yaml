apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frink-embedding-ui.fullname" . }}-test-connection"
  labels:
    {{- include "frink-embedding-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frink-embedding-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
