apiVersion: v1
kind: Service
metadata:
  name: {{ include "frink-embedding-ui.fullname" . }}
  labels:
    {{- include "frink-embedding-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frink-embedding-ui.selectorLabels" . | nindent 4 }}
